#pragma once

#include <string>

// Update once repURL changes [duh]
const std::string repoUrl = "https://github.com/BEG5210/Exposure-Variance-Calculator.git";

// Default camrea settings values. To be updated into the camera settings class.
const float defaultShutterSpeed = 1.0f;
const float defaultApeature = 1.0f;
const int defaultIso = 100;